import Detail from "@/containers/rocket/detail";
import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "AlloBank - SpaceX | Detail Rocket",
  description: "AlloBank SpaceX project FE detail page",
};

const PageDetailRocket = () => {
  return <Detail />;
};

export default PageDetailRocket;
